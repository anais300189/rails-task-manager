<h1>My tasks</h1>
<% @tasks.each do |task| %>
<p><%= task.completed %>
  <%= link_to task.title, task_path(task.id) %>
  </p>
  <p><%= link_to "Delete this task", task_path(task.id), method: :delete,
            data: { confirm: "Are you sure?" }%></p>
<% end %>

